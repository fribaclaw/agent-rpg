{
  "name": "agent-rpg-v2",
  "version": "2.0.0",
  "private": true,
  "description": "A 3D RPG dashboard for configuring OpenClaw agents",
  "scripts": {
    "dev": "npm run dev --prefix apps/api",
    "build": "npm run build --prefix packages/types && npm run build --prefix apps/api",
    "install:all": "npm install && npm install --prefix packages/types && npm install --prefix apps/api",
    "db:generate": "npm run db:generate --prefix apps/api",
    "db:migrate": "npm run db:migrate --prefix apps/api",
    "db:studio": "npm run db:studio --prefix apps/api"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}